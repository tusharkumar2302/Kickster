import { s, vs } from 'react-native-size-matters';

const spacing = {
  xs: s(4),
  sm: s(8),
  md: s(16),
  lg: s(24),
  xl: s(32),

  vertical: {
    sm: vs(8),
    md: vs(16),
    lg: vs(24),
  },
};

export default spacing;
